# ============================================================
# ONE-TIME BOOTSTRAP -- creates the S3 bucket + DynamoDB table
# that will hold Terraform's remote state going forward.
#
# Run this ONCE, by itself, in its own folder
# ============================================================

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

resource "aws_s3_bucket" "terraform_state" {
  bucket = "nthnz-terraform-state"  # must be globally unique -- change if taken

  # Prevent accidental deletion of this bucket via terraform destroy --
  # losing this would mean losing state file
  lifecycle {
    prevent_destroy = true
  }
}

# Versioning means if the state file ever gets corrupted or overwritten
# badly, can roll back to a previous version.
resource "aws_s3_bucket_versioning" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id
  versioning_configuration {
    status = "Enabled"
  }
}

# Encrypt the state file at rest -- it can contain sensitive resource
# details
resource "aws_s3_bucket_server_side_encryption_configuration" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "AES256"
    }
  }
}

# Block all public access -- this bucket should NEVER be public.
resource "aws_s3_bucket_public_access_block" "terraform_state" {
  bucket = aws_s3_bucket.terraform_state.id

  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# DynamoDB table for state locking -- prevents two `terraform apply`
# runs from happening at the same time and corrupting the state.
resource "aws_dynamodb_table" "terraform_locks" {
  name         = "terraform-state-locks"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"

  attribute {
    name = "LockID"
    type = "S"
  }
}

output "state_bucket_name" {
  value = aws_s3_bucket.terraform_state.bucket
}

output "lock_table_name" {
  value = aws_dynamodb_table.terraform_locks.name
}
